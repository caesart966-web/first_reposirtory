# CLAUDE.md — путеводитель по репозиторию

В репозитории несколько независимых проектов:

- **`orders-app/`** — PWA «Заказы»: учёт заказов фрилансера (React + Vite + TypeScript +
  Tailwind + Dexie/IndexedDB, офлайн, публикуется на GitHub Pages через
  `.github/workflows/deploy-orders-app.yml`). Структура, правила и инструкции —
  в `orders-app/README.md`. Ключевое правило домена: комиссия хранится снимком
  (`commissionRate`) в самом заказе, поэтому изменение процента у источника
  не меняет уже созданные заказы; производные величины (комиссия, «на руки»,
  остаток, дни до срока) не хранятся, а считаются в `src/lib/calc.ts`.
- **`master-site/`** — сайт строительной компании ООО «МАСТЕР» (Ростов-на-Дону):
  одностраничник на чистой статике (HTML + CSS + vanilla JS, без сборщиков), весь
  контент и все фотографии взяты из PDF презентации компании. Инструкция для
  заказчика — `master-site/README.md`, выписка фактов из презентации —
  `master-site/CONTENT.md`, решения по оформлению — `master-site/DESIGN.md`.
  Ключевое правило: на сайт не попадает ничего, чего нет в презентации.
- **`sro-site/`** — рекламный сайт по вступлению в СРО (ООО «БИЗНЕС-ГРУПП», https://biznesgrp.ru;
  текст от первого лица — речь специалиста компании; React + Vite + TypeScript + Tailwind +
  lucide-react; главная, три страницы видов СРО, семь страниц услуг). Все реальные данные,
  которых нет, — плейсхолдерами вида `[ЦЕНА]`, и на экран они не выводятся; единая точка
  контактов — `sro-site/src/content/contacts.ts`. С 24.09.2026 формы нет: квиз, приём заявок
  (`lead.ts`, `api/submit.php`) и политика конфиденциальности сняты по просьбе заказчика,
  связь — раздел «Связаться» (`Contact.tsx`, `#contacts`) внизу каждой страницы. Оформление:
  тёплая палитра без синего (графит, крем, латунь), Brygada 1918 в заголовках и Onest в тексте
  (Playfair, Inter и Montserrat отброшены нарочно — шрифты по умолчанию у сайтов от нейросетей),
  медленные переходы с `prefers-reduced-motion`. Первый экран с 26.09.2026 — «лист и окно»
  вместо слайдера: на бумаге заголовок, кнопки и три вида СРО списком (данные — `TYPES_GROUP`
  из `nav.ts`, как в меню), справа кадр с краном растворяется в бумаге и при загрузке
  «проявляется»; на телефоне заголовок, обе кнопки и все три вида видны без прокрутки при
  видимой высоте окна 780 px — мерится по окну браузера, а не по экрану, и после появления
  (`scripts/test-site.mjs`). **Все фотографии — в тёплом монохроме** (`scripts/monotone.py`,
  с 26.09.2026): яркость в градиент «графит → бумага», уровни у каждого кадра свои, кран —
  светлым ключом; кадры видов увеличены Real-ESRGAN (`scripts/prepare-slide-photos.py`).
  Номера (01, 02…) — только там, где есть порядок: шаги работы и опись документов.
  Фемида — фоном тёмного раздела «О нас» (с 26.09.2026; «Как проходит работа» — без фото),
  в «Документах» зеркально — папки фоном во всю высоту раздела (исходник пока превью), те же
  папки — в шапке услуги «Подготовка документов». Переходы между страницами — только CSS
  (`@view-transition`, 0,4 с, кадр с главной перетекает в шапку вида); скрипт страницы
  `blocking="render"` и первая отрисовка через `flushSync`, иначе переход снимал бы пустой
  лист — стережёт `test-site.mjs` с задержкой скриптов.
  Переходы понятны заранее: семь карточек услуг берутся из `content/services.ts` (те же
  данные, что страницы и меню, под названием — подсказка `hint`), типовые ситуации ведут
  на страницы услуг, у внутренних страниц — оглавление «На этой странице» в шапке и
  `PageExtras.tsx` («Сколько стоит» без цифр и «Услуги по теме»). Движение по прокрутке —
  только CSS (`.scroll-drift`, `.scroll-settle`), сайт от него не тяжелеет.
  Знак компании — весы под фронтоном (выбор заказчика 25.09.2026), одни координаты
  в четырёх местах: `ScalesMark`, `favicon.svg`, `404.html`, `build-og.mjs`.
  Контраст надписей поверх фотографий мерится по пикселям (`scripts/test-hero-contrast.mjs`),
  сайт целиком — `scripts/test-site.mjs`. Ключевое правило: никаких выдуманных фактов,
  цифр, цен и сроков. Инструкции — `sro-site/README.md`.
- **`norma-site/`** — многостраничный сайт «НОРМА» по вступлению в СРО (ИП Багишев А. А.;
  **адрес превью: https://caesart966-web.github.io/first_reposirtory/norma/** —
  публикуется из `main` через `.github/workflows/deploy-orders-app.yml`, не отдельным
  файлом workflow, а шагами внутри него;
  Astro 7, статическая генерация, без фронтенд-фреймворков в браузере). Отстройка строится
  на двух вещах: **эталонная точность** (у каждого факта — чип со статьёй закона, у страниц
  и статей — штамп «Проверено» с датой) и **честность** (калькулятор «Нужна ли вам СРО»
  умеет отвечать «не нужна» со ссылкой на норму). Единые точки правды:
  `src/config/site.ts` — контакты и реквизиты, `src/config/facts.ts` — нормы и таблицы
  взносов, `src/config/services.ts` — список услуг и группы, `src/config/offer.ts` —
  условия вступления, `src/content/articles/` — статьи.
  Ключевые правила: порог для строителей — 10 млн ₽ (не 3 млн), «допусков СРО»
  не существует с 2017 года, региональный принцип действует только для строителей,
  КФ ОДО раскрывается отдельной строкой.

  **Главное предложение** — пять условий вступления в `src/config/offer.ts`
  (`SpecialOffer.astro`, карточка на первом экране). Это условия конкретных СРО,
  а НЕ нормы закона: **чипов со статьями рядом с ними быть не должно**.
  Оговорка «это условия СРО, а не нормы закона» стояла внизу карточки
  отдельным блоком с восклицательным знаком — заказчик попросил убрать:
  на красной карточке она читалась предупреждением об опасности и занимала
  на телефоне пол-экрана. Разделение коммерческих условий и норм закона
  осталось правилом: то же самое объясняет `FEES_NOTE` из `config/fees.ts`
  в разбивке взносов на главной и на странице «Стоимость» — этот текст
  трогать нельзя, иначе различие пропадёт с сайта совсем. Отдельное предложение про бесплатный сайт живёт
  на странице `uslugi/marketing.astro`.

  **Взносы СРО** — `src/config/fees.ts`, отдельно от `facts.ts`. В facts.ts
  компенсационные фонды: их размеры в законе, рядом стоит статья. В fees.ts
  вступительный, членский, целевой взносы и страхование: закон их не
  устанавливает, чипа со статьёй рядом быть не должно. Сейчас: строителям
  вступительный 0 ₽, членский 5 000 ₽ в месяц, целевой в НОСТРОЙ
  10 000 ₽; проектировщикам и изыскателям 0 / 5 000 в месяц / 10 000 в НОПРИЗ;
  страхование в первый год не требуется. Членский взнос помесячный — 5 000 ₽
  в месяц, 60 000 ₽ за первый год. Целевой взнос проектировщиков
  заказчик сначала назвал с оговоркой «вроде» (8 000 ₽), затем уточнил
  и подтвердил: в НОПРИЗ это 10 000 ₽.
  **Суммы в fees.ts и пять условий в offer.ts обязаны сходиться**: один раз
  они разошлись (карточка обещала вступительный 0 ₽ и первый год без членских,
  а в разбивке у проектировщиков стояли 8 000 и 5 000).

  Оформление — **«печать и бумага»**. Белый лист, тёплая тонкая линейка,
  антиква и одно яркое пятно: красная печать. Синего на сайте нет ни одного
  и быть не должно — это осознанная отстройка от рынка, где у всех синий
  градиент и стоковые каски. Акцент — сургучный `#B0202B` (цвет печати на
  документе), бумага `#FFFFFF` и тёплая `#F6F3EC`, тёмные секции `#171310`
  (тёплый графит, не синий).

  Шрифтов два: **Literata** — заголовки и текст статей (антиква для чтения
  с экрана: сайт говорит о законе и документах, и антиква говорит о том же);
  **Golos Text** — интерфейс, кнопки, подписи, таблицы, цифры. Курсив есть
  только у Literata. Заголовки набираются **обычным регистром, не капсом**.

  **Фотография на первом экране** — `SITE.heroPhoto` (сейчас подписание
  документов, `public/img/signing.webp`). Лежит за текстом, приглушена и замаскирована так,
  чтобы читаться фактурой листа: цвет уводится, кадр гаснет слева под
  колонкой заголовка и растворяется сверху и снизу. Порог переключения
  раскладки фотографии — 1023 px, тот же, что у самой сетки первого экрана.
  Та же фотография стоит во всех тёмных шапках разделов — `SITE.headPhoto`
  и компонент `HeadPhoto.astro` (19 адресов). Обработка там противоположная:
  на белой бумаге кадр высветляют, на тёмном графите приглушают и
  притемняют, иначе светлый текст ложится на светлые пятна. Слой описан
  в `global.css` у `.head-photo`, а не в компоненте: правило должно
  доставать до шапки из любой страницы.

  У каждой шапки **свой `position`** — фотография одна на весь сайт, и без
  сдвига кадра разделы выглядели бы одной картинкой, размноженной двадцать
  раз. Проп `soft` приглушает слой там, где в правой колонке стоит
  содержимое: на контактах кадр приходится ровно под подписи бланка,
  и на полной непрозрачности они давали 3.6:1 при норме 4.5.

  Меняете фотографию или маску — обязательно `node scripts/test-hero-photo.mjs`:
  он снимает настоящие пиксели под каждой строкой на трёх ширинах и требует
  4.5:1 — 255 замеров на 19 адресах. Список шапок в нём записан поимённо,
  а не собирается обходом сайта: проверка обязана падать, когда фотографию
  поставили на новую страницу и забыли вписать её туда. Замер идёт по полю с буквами, без рамки
  и отступов: у штампа «Сверено» рамка того же зелёного цвета, что и текст,
  и по внешнему прямоугольнику давала ложные 3.8:1. Статический расчёт контраста этого не видит (в стилях там белая
  бумага) — при первой установке абзац лёг на бронзу и давал 1.5:1.

  Строительная тема идёт линиями, а не фотографиями: размерные линии
  с засечками между секциями (`.section + .section::before`, цитата из
  чертежа) и вычерченный контур дома с краном на первом экране (показывается
  только когда `SITE.heroPhoto` пуст).

  **Фон первого экрана** — три слоя в `Hero.astro`: `.paper-light` (тёплый
  свет из правого верхнего угла и лёгкая тень снизу), `.paper-grain` (зерно
  бумаги — SVG-turbulence с `stitchTiles`, поэтому повторение не читается)
  и `.rosette` — гильош, розетка из окружностей, расставленных по кругу.
  Здесь была миллиметровка — клетка 22 и 110 px; заказчик попросил убрать:
  клетка читается школьной тетрадью и спорит с фотографией. Гильош уходит
  за левый край и уравновешивает фотографию справа; двойная маска (круглая
  плюс продольная) уводит его от заголовка — под крупной антиквой частые
  тонкие линии рябят. Ниже 1023 px розетка скрыта: в одну колонку текст
  идёт во всю ширину.
  Меняете фон — прогоняйте `node scripts/test-hero-photo.mjs`: он меряет
  настоящие пиксели под каждой строкой. Стоковых фотографий с людьми в касках на сайте быть не должно —
  с каской в кадре сайт становится неотличим от соседних. Видео убрано полностью:
  ни файлов, ни скриптов сжатия.

  **Имя «НОРМА» пересматривали и оставили** (решение заказчика, 25 сентября
  2026). Довод за: имя совпадает с идеей сайта и со знаком-монограммой «Н»,
  который заказчик выбрал сам из своего набора. Довод против был один
  и настоящий: «норма» — обычное слово этой же темы, оно стоит 87 раз
  на 26 страницах сайта в обороте «норма закона», поэтому брендовый запрос
  «норма» ведёт куда угодно, только не сюда. **Закрывается это доменом,
  а не переименованием**: адрес обязан содержать и имя, и предмет
  (`norma-sro.ru`), разбор записан в README. Рассматривались и отклонены
  имя собственное («Багишев» — уникально и совпадает с тем, как о нём
  говорят клиенты, но требует нового знака) и слово из мира документа
  («Оттиск», «Сургуч», «Буква» — уникально и в тон оформлению, но смысл
  приходится объяснять клиенту). Если к вопросу вернутся: замена самого
  имени механическая (26 заголовков страниц, `site.ts`, `seo.ts`,
  `make-og.mjs`), дорого стоит не она, а перерисовка знака и значков.

  **Фирменных знака ДВА, и на сайте второй.** В `norma-site/brand/` лежит
  архив первого варианта — монограмма «Б» с колонной, цвет `#03213D`
  (`logo-original.pdf/png`, `logo-mark.svg`). Заказчик прислал несколько
  логотипов и выбрал из своего набора другой — тот, в котором читаются «Н»
  и латинская N; он и стоит на сайте, причём разметкой, а не картинкой
  (`LogoMark.astro` плюс `public/favicon.svg`, менять надо оба).
  README архива утверждал, что на сайте стоит `logo-mark.svg`, — устарело
  и опасно: взяв его для визитки, поставили бы не тот знак. Исправлено.
  Готовые файлы действующего знака собирает **`npm run brand`**
  (`scripts/make-brand.mjs`): знак вектором и растром, горизонтальный
  логотип, светлая версия, PDF для типографии, аватар и шапка письма.
  Цвета берутся из `global.css`, телефон и название — из `site.ts`,
  контуры — из `LogoMark.astro`: положенный в папку логотип разошёлся бы
  с сайтом при первой правке телефона, и заметить это было бы некому.
  Логотип снимается ЭЛЕМЕНТОМ, а не страницей: на холсте фиксированного
  размера содержимое уезжало в левую половину файла. У `svg` там
  `display:block` — строчный элемент оставлял место под выносной элемент
  строки, и квадрат 512 сохранялся как 512×518.

  **Печать «сверено»** (`Seal.astro`) — фирменный знак: круглый оттиск
  с датой из `SITE.checkedDate`. Ставить её рядом с условиями вступления
  НЕЛЬЗЯ: она означает «сверено с законом», а пять условий — условия
  конкретных СРО, а не нормы закона.

  Движение: чертёж рисуется линия за линией, печать «прикладывается»,
  блоки выплывают со ступенчатой задержкой, шапка подбирается при прокрутке,
  ответы в вопросах раскрываются через `::details-content` + `interpolate-size`.
  Всё отключается при `prefers-reduced-motion`. Длительности и кривая берутся
  из `--t-fast/--t-mid/--t-slow` и `--ease`, а не пишутся по месту.

  Ритм главной задаёт класс `section--split`: заголовок раздела уходит в левую
  липкую рейку, содержимое — в правую колонку (обязательно внутри `.sbody`,
  иначе липкость не работает). Фон чередуется: `section--paper` — тёплый лист,
  `section--dark` — тёмный. Тёмные шапки страниц — всегда класс `.page-head`,
  своих имён заводить нельзя: на них написаны общие правила.

  **Страница контактов** (`src/pages/kontakty.astro`) — карточка контрагента,
  а не футер: номер размером с заголовок, у каждого канала объяснение «зачем он»,
  реквизиты с кнопкой «копировать» и ссылками в государственные реестры, печать
  «Сверено». Карты нет и не будет — офиса нет, показывать нечего; вместо неё
  раздел «География». Круглых значков соцсетей и карточек с тенями там быть не
  должно: разделители — линейки в 1 px. Строка «Сейчас на связи» считается по
  московскому времени и при круглосуточном режиме сообщает не «открыто/закрыто»
  (такой индикатор всегда зелёный и потому не значит ничего), а что быстрее
  сейчас — позвонить или написать. Полоса «Написать» в первом экране живёт
  только на телефоне (до 899 px) и занимает место бланка: строки каналов
  высокие, и без неё до первого мессенджера было полтора экрана прокрутки.
  Логику формы внизу страницы правки контактов не трогают.

  **Плавность — не украшение, а измеряемая вещь.** `npm run audit`
  (`scripts/audit.mjs`) снимает вес, FCP, LCP, сдвиг вёрстки и число
  пропущенных кадров при прокрутке шагами по трети экрана. Три решения
  держатся на его показаниях:
  шапка при прокрутке **не меняет высоту** — раньше уменьшались отступы,
  и вся страница под липкой шапкой прыгала на 8 px, а `padding`
  пересчитывал раскладку каждый кадр перехода; теперь подбирается
  содержимое (`transform: scale`), высота всегда 79 px;
  предзагружается **только кириллица** — латиница весит 60 КБ из 98 и на
  критическом пути отнимала канал у начертаний, которыми набрана вся
  страница (LCP на медленном канале 2236 → 1700 мс);
  появление блоков запускается **до входа в кадр** (`rootMargin` +12%,
  ступенька 55 мс) — при быстрой прокрутке содержимое доезжало
  проявляться уже после остановки.

  **База знаний — 27 статей** в `src/content/articles/`. Каждая выходит
  со штампом «Сверено с законом» и датой из своего `updated:`.
  Двадцать седьмая — `sro-po-vidam-rabot.md` от 15 сентября 2026: отвечает
  на запросы «нужна ли СРО для электромонтажа / вентиляции / кровли»,
  которых на сайте не было ни одного, и не плодит под них страницы-клоны.
  Новых норм не вводит — сводит вместе уже разобранные, — но два её
  утверждения внесены в `SVERKA.md` отдельными пунктами: что у члена СРО
  нет деления прав по видам работ и что договор с физлицом-собственником
  обязанности членства не порождает. Двадцать
  статей от 7 сентября 2026 написаны по нормам ГрК РФ, но дословно
  с текстом кодекса не сверялись: правовые базы из среды сборки недоступны.
  Список всех утверждений, которые надо пройти перед боевым доменом, —
  в `norma-site/SVERKA.md`. 8 сентября 2026 по нему прошла сверка
  по вторичным источникам (правовые базы из среды сборки закрыты — все
  до одной): 15 пунктов закрыто, 23 осталось. Главное, что она нашла:
  **309-ФЗ от 31.07.2025 действует с 1 марта 2026 года**, и сайт о нём
  не знал — член СРО теперь уведомляет её о каждом договоре подряда
  за три рабочих дня, а не раз в год. Написана статья
  `uvedomleniya-sro-309-fz.md`. Там же исправлено: **ПП РФ № 559
  от 11.05.2017 утратило силу с 01.09.2024**, действующее основание
  требования о двух специалистах — ч. 6 ст. 55.5 ГрК РФ
  (`LAW.twoSpecialists`). `SITE.checkedDate` = 8 сентября 2026 — поднята
  решением заказчика после этой сверки (я её не трогал: сверка шла
  по вторичным источникам). Тогда же строка на странице базы знаний
  сменилась с «Все материалы сверены» на «Нормы сверены»: у статей даты
  свои, часть осталась прежней, и общий штамп не должен говорить за них. Правило то же, что на страницах охраны труда
  и промышленной безопасности: **отсутствие факта дешевле факта,
  за который нельзя ответить** — сомнительное утверждение убирается,
  а не смягчается.

  **Робот-наблюдатель** — `.github/workflows/watch-law-norma.yml` плюс
  `scripts/watch-law.mjs`, раз в неделю по понедельникам. Снимает отпечаток
  текста норм (10 источников, у каждого зеркала-запасные) и следит за датой
  действующей редакции ГрК — именно она первой показывает, что вышел закон,
  меняющий кодекс: так проглядели 309-ФЗ, работавший полгода. Отпечаток
  хранится **по адресу**, а не по норме: у зеркал разное обрамление
  страницы, и сравнение между ними давало бы ложную тревогу. Молчание
  источника не считается успехом — робот отдельно пишет «не смог
  проверить». Если тихо, задача не заводится вовсе: еженедельное
  «всё в порядке» перестают читать.
  Робот **сначала пробует адрес, по которому норму снимали в прошлый раз**,
  и только потом остальные зеркала. Без этого он на втором же запуске увёл
  четыре нормы из десяти на запасные источники — а отпечаток хранится
  по адресу, и сравнивать стало не с чем. Норма, снятая впервые по новому
  адресу, попадает в отдельную корзину «сверить не с чем» и НЕ считается
  сверенной: раньше она уходила в `same`, и робот писал «ни одна из 10 норм
  не менялась», отчитываясь за работу, которой не делал.

  **Робот НЕ правит статьи и НЕ двигает `SITE.checkedDate`** — и это
  не недоделка. Печать «Сверено с законом» — обещание владельца сайта
  клиентам; если её двигает скрипт, она означает «скрипт отработал».
  Правка текста по различию в объёме — способ опубликовать неверный совет
  под штампом «проверено». Робот показывает пальцем, решает человек.
  Он же напоминает, если печати больше 90 дней.

  **Членский взнос — помесячный**, `FEES.memberMonth` = 5 000 ₽. Раньше поле
  называлось `memberFirstYear` и означало «5 000 ₽ за весь первый год» —
  заказчик уточнил, что это стоимость месяца, и сайт занижал цену
  в двенадцать раз на первом экране, в итоге на главной и в разбивке
  на «Стоимости». Поле переименовано, а не просто переписано числом:
  старое имя само подсказывало неверный смысл. Год считается как
  `memberMonth * 12`, помощники — `memberFor()` и `memberYear()`.
  В калькуляторе на «Стоимости» есть выбор периода (`MEMBER_PERIODS`:
  1/3/6/12 месяцев) — **без номера шага и отдельным блоком в итоге**:
  шаги 1-4 считают установленное законом и одинаковое у всех СРО,
  а членский взнос назначает сама СРО. Сложить их в одно число значило бы
  выдать коммерческое условие за норму.

  **Таблицы на телефоне разворачиваются в карточки.** Ниже 600 px строка
  `table.data` перестаёт быть строкой: сверху уровень, ниже значения
  с подписями из `data-label` у ячеек, шапка прячется приёмом «видно только
  голосом». До этого таблицы стояли в контейнере с горизонтальной
  прокруткой: четыре колонки с суммами в 390 px не помещаются, формально
  всё работало — практически правую половину не видели, потому что ничто
  не сообщало, что таблицу надо листать вбок. **Каждой новой ячейке нужен
  `data-label`**, иначе в карточке она останется без подписи.
  Стережёт `scripts/test-tables.mjs`: обходит все таблицы сайта на трёх
  ширинах и проверяет, что таблица не шире экрана, не требует прокрутки
  внутри контейнера, текст не вылезает из ячеек и ячейки не наезжают
  друг на друга. Скрытые элементы он отличает по попаданию в точку центра —
  спрятанная шапка сохраняет размеры и иначе считалась бы наездом.

  **Тёмная таблица (`.tbl-wrap.dark`) красится общими правилами** в `global.css`,
  как и `.page-head`, и по той же причине: пока тёмная таблица была одна
  (на «Стоимости»), набор лежал у неё в scoped-стилях; вторая — разбивка
  взносов на главной — получила бы тёмные чернила на тёмном графите.
  Там же завёлся `--good-bright`: зелёный `--good` подобран под белую бумагу
  и на графите даёт 1,9:1, а «0 ₽» в разбивке набрано зелёным.
  Ширины колонок этой таблицы заданы **только от 600 px**: ниже строка
  разворачивается в карточку, ячейки становятся блоками, и заданная ширина
  зажимает их в 46 % экрана — правка «чтобы не переносилось» на компьютере
  один раз уже удлинила раздел на телефоне с 2,75 до 3,58 экрана.

  **Таблицы в статьях** приходят из markdown голым `<table>` — оформление
  им даёт `.prose :global(table)` в `[...slug].astro`, повторяя `table.data`
  с остальных страниц. На телефоне у таблицы включается `table-layout: fixed`:
  без него самая длинная надпись в шапке растягивала таблицу до 399 px
  на экране в 390, правый столбец обрезался, а страница получала
  горизонтальную прокрутку. Там же шапка теряет капс: капс в русском шире
  строчных на четверть и в колонке в треть экрана не помещается ни при каком
  кегле. Длинные слова в шапке размечаются мягким переносом (`\u00ad`) —
  иначе запасной `overflow-wrap: break-word` рвёт их посреди слога.
  Страница `/baza-znaniy/regionalnyy-princip/` внесена в `test-widths.mjs`
  именно как страница с таблицей.

  Осторожно с YAML в шапках статей: значение с двоеточием и пробелом
  (`title: Уровни: как выбрать`) ломает сборку — такие строки берутся
  в кавычки.

  **`npm run facts`** (`scripts/check-facts.mjs`) — сверка сайта с самим собой.
  Не с законом: правовые базы из среды сборки закрыты сетевой политикой
  (pravo.gov.ru, consultant.ru, garant.ru, cntd.ru — все отвечают 403),
  и сверка с кодексом остаётся ручной по `SVERKA.md`. Проверка ловит другое,
  и это самый опасный класс ошибок здесь: **сайт, противоречащий сам себе**.
  Она ищет отменённые формулировки (порог 3 млн, «допуск СРО», ПП 1/29,
  ст. 212 и 225 ТК, ПТМ), сверяет числа со страниц со значениями из
  `facts.ts` и `fees.ts` и следит, чтобы одно утверждение везде опиралось
  на одну норму. Отменённые нормы сайт называет нарочно — чтобы их
  опровергнуть, — поэтому проверка ругается только там, где рядом нет
  опровержения. Ею уже поймано настоящее противоречие: новая статья
  про технического заказчика утверждала, что членство ему не нужно,
  а сравнительная таблица на сайте — что нужно.

  **Ссылки внутри статей чинятся на выводе, а не в самих статьях.**
  Сайт живёт в подпапке, и ссылки из компонентов получают её через `href()`.
  Ссылки, написанные в markdown, через `href()` не проходят: `/proverit-sro/`
  так и уходит в разметку и на боевом адресе ведёт в корень домена. Так
  и случилось — 29 битых ссылок, заказчик получил «Site not found» от GitHub.
  Правится в `[...slug].astro`: готовая разметка статьи берётся из записи
  коллекции и прогоняется через `withBase()`. Чинить в самих статьях нельзя —
  подпапка зависит от того, куда собирают. Стережёт `npm run check:base`:
  собирает с тестовой подпапкой и требует, чтобы **каждая** внутренняя
  ссылка с неё начиналась. Обычная проверка ссылок этот класс ошибок
  не видит принципиально — она гоняется на локальном превью, где подпапки
  нет и такие ссылки работают.

  **Поиск по сайту** — `src/lib/search.ts` (морфология и ранжирование),
  `scripts/make-search-index.mjs` (индекс собирается ПОСЛЕ сборки, из готовых
  страниц: так в него попадает ровно то, что видит посетитель), страница
  результатов `src/pages/poisk.astro`, строка в шапке — в `Header.astro`.
  Индекс грузится при первом обращении к поиску, а не вместе со страницей:
  он весит больше, чем вся остальная страница.
  Морфология — стеммер Портера, плюс совпадение по общему началу основ
  не короче четырёх букв: «вступить» даёт основу «вступ», «вступление» —
  «вступлен», и точным сравнением их не свести.
  Ранжирование — BM25, и обе его поправки здесь обязательны: **редкость
  слова** («СРО» стоит на всех 44 страницах и не различает ничего)
  и **длина страницы** (главная и оглавления говорят понемногу обо всём).
  Без них по запросу «допуск СРО» первой шла оглавительная страница базы
  знаний, а по «документы для вступления» — главная.
  Обвязка страниц помечена **`data-nosearch`** (форма заявки, крошки,
  «другие услуги», «читайте дальше») — иначе по слову «заявка» находился бы
  весь сайт. Новый повторяющийся блок нужно помечать так же.
  Разметку результатов вставляет скрипт, поэтому её стили пишутся
  **через `:global()`**: областью видимости компонента до вставленного
  не достать.
  Стережёт `scripts/test-search.mjs`: пары словоформ, четырнадцать запросов
  с записанной страницей-ответом, работа в браузере, и отдельно — что
  с любой страницы на любой ширине есть куда уйти. Последнее не случайно:
  **с 768 до 999 px на сайте не было навигации вообще** — гамбургер прятался
  с 768, а горизонтальное меню приходило с 1000. С тех пор оба правила
  двигаются только вместе; сейчас порог 1100 px.

  **Городские страницы** — `src/config/regions.ts` (данные), маршрут
  `src/pages/sro/[gorod].astro`, оглавление `src/pages/sro/index.astro`.
  **Городов ТРИ — Петербург, Москва, Ростов-на-Дону, — и это решение
  заказчика.** Было тридцать три, под запросы вида «вступить в СРО
  в Екатеринбурге». Заказчик их снял: партнёрские СРО у него в трёх
  городах, а страница «Вступление в СРО в Барнауле» обещала местную
  работу, за которую отвечать нечем. Цена названа ему вслух и записана
  в README: сайт больше не отвечает на городские запросы по тридцати
  городам, а это была вся его региональная выдача. Удалённые лежат
  в истории git — коммит «Города: три вместо тридцати трёх».

  Взамен мысль «работаю по всей России, СРО вашего субъекта подбираю
  отдельно» повторена в ТРЁХ местах, и все три обязаны остаться: блок
  «Остальные регионы — так же» на главной, раздел «А если ваш регион
  не в списке» на `/sro/` и строка в показаниях карты. Без них раздел
  читается как «работаю только в трёх городах», и посетитель из четвёртого
  уходит, решив, что ему откажут.

  У каждого города СВОЙ разбор: субъект РФ и его код, соседний субъект,
  с которым путают, и чем путаница кончается.
  **Это единственное, что отличает такую страницу от дорвея**, а дорвеи
  Яндекс наказывает сайтом целиком. У Калининградской области соседних
  субъектов нет вовсе (`neighbours: []`) — вместо списка страница объясняет,
  почему регионального правила это не отменяет; проверка требует именно
  объяснения, а не пропуска раздела. Стережёт `scripts/test-regions.mjs`:
  меряет долю текста, которого нет ни на одной другой городской странице
  (сейчас 27–35 % при пороге 18 %; клон даёт 11 % и валит проверку вместе
  с исходной страницей). Он же требует, чтобы страница не была тупиком:
  ссылки в разбор регионального принципа, в стоимость и в проверку реестров.
  Выдуманных региональных цифр (сколько СРО, почём, за сколько дней) там нет
  и быть не должно; наоборот, прямо сказано, что компенсационные фонды
  установлены законом и по стране одинаковы.
  Фотография в шапке города — поле `photo`, показывается только если файл
  реально лежит в `public/img/` (проверка `existsSync` в шаблоне): иначе
  в разметку уходил бы `background-image` на несуществующий файл, а браузер
  о таком молчит.
  **СРО-партнёры** — `src/config/partners.ts`, раздел «С кем я работаю»
  на страницах Петербурга, Москвы и Ростова-на-Дону (`#partnery`). Семь
  организаций из КП заказчика, с регистрационными номерами: номер
  открывается в государственном реестре, и это единственная причина,
  по которой список вообще можно ставить на сайт. Дата «в реестре с …»
  **вычисляется из номера** (последние восемь цифр — дата внесения),
  вид СРО — из буквы С/П/И: отдельными полями они разошлись бы с номером.
  Из КП НЕ перенесены: колонка «Взнос в КФ» (это ст. 55.16 ГрК РФ, а не
  условие СРО — суммы совпадают с `FUNDS` до рубля, и в карточке партнёра
  закон выглядел бы его личным условием); оценки «одна из старейших»,
  «солидное», «динамично развивающаяся» (проверить нельзя, а «одна
  из старейших» ещё и стояло рядом с «основана в 2019 году»); «входит
  в структуры НОСТРОЙ» (членство в национальном объединении обязательно
  для всех СРО своего вида — как признак выбора это вводит в заблуждение).
  Целевой взнос в КП указан 8 000 ₽ — заказчик подтвердил, что это ошибка:
  и в НОСТРОЙ, и в НОПРИЗ 10 000 ₽; сумма берётся из `fees.ts`
  и в карточки не переписывается.
  **Три СРО из восьми работают не на условиях с первого экрана**: у ЯРД
  вступительный 5 000 ₽ вместо 0 и страхование не требуется вовсе;
  у ОРС ещё и членский 10 000 ₽ в месяц вместо 5 000; у АСО «МСК»
  членский 10 000 ₽ и **нет целевого взноса в НОСТРОЙ** — отличие
  в пользу вступающего показывается так же, как отличие не в его пользу,
  иначе карточка перестаёт быть условиями конкретной организации.
  Отличия хранятся отдельным полем `fees` у партнёра (`entry`,
  `memberMonth`, `target`), шаблон сам метит такие строки звёздочкой
  и выводит объяснение. Прятать их нельзя: ростовский строитель увидел бы
  5 000 на первом экране и 10 000 в СРО — и весь сайт, построенный
  на проверяемости, перестал бы чего-либо стоить.
  Стережёт `npm run facts` (раздел «Партнёрские СРО»). Проверка разбирает
  **карточку каждой организации**, а не страницу целиком, и сверяет пару
  «подпись — значение»: первая версия искала сумму по всему тексту
  страницы и пропустила настоящую подмену — у АСО «МСК» целевой взнос
  0 ₽, шаблон подставил общие 10 000 ₽, а «0 ₽» нашлось в строке
  вступительного взноса той же карточки. Пометка ищется по **звёздочке
  в подписи, а не по классу строки**: класс задаёт только цвет, читателю
  он не виден, и проверка по классу пропускала подделку, где звёздочка
  исчезала, а класс оставался. Проверено тремя подделками: подмена суммы,
  снятая звёздочка при настоящем отличии и звёздочка там, где отличия нет,
  — каждая роняет проверку с именем организации и названием строки.
  У города без партнёров раздела нет вовсе — придумывать организацию
  под город нельзя.
  **Пустой `reg` = номер не подтверждён**, и такая запись на сайт
  не выходит (`partnersOf` её отфильтровывает), но из конфигурации
  не выбрасывается: `npm run facts` печатает о ней напоминание при каждом
  запуске. Механизм завёлся на АСО «МСК» (`asomsk-sro.ru`): номер сам
  я получить не смог — сайт организации, `reestr-sro.ru` и `all-sro.ru`
  закрыты сетевой политикой среды (EGRESS_BLOCKED), а поиск выдавал
  похожее сокращение другой организации, АСК «МСК». Номер прислал
  заказчик, карточка опубликована.


  **Меню на телефоне кроет весь экран, а прокрутку фона держит фиксация
  body.** Было: панель `inset: 68px 0 0` плюс `body { overflow: hidden }`.
  Оба решения неверны. Одного `overflow` на `body` телефон не слушает —
  прокручивается `<html>`, и страница ехала за меню; а сам `overflow: hidden`
  ломает `position: sticky`, из-за чего шапка уезжала вместе с документом
  (на замере с прокруткой 1123 px она оказывалась на −1923 px) и в щель
  в 68 px было видно страницу — на скриншоте заказчика красная полоса
  над меню. Теперь: `html.menu-open` фиксирует `body` на запомненном
  смещении, шапка прижимается `position: fixed`, панель идёт `inset: 0`.
  Возврат прокрутки при закрытии — **`behavior: 'instant'`**: у `html`
  стоит `scroll-behavior: smooth`, и обычный `scrollTo` проматывал бы
  страницу назад на глазах.
  Панель лежит ВНУТРИ `<header>`, поэтому с `inset: 0` она накрыла
  гамбургер: позиционированный элемент рисуется поверх непозиционированных
  соседей. Строка шапки поднимается над ней (`html.menu-open .hdr`).
  Стережёт `scripts/test-a11y.mjs`. Прежняя проверка там смотрела
  на `document.body.style.overflow === 'hidden'` — то есть на записанное
  свойство, а не на поведение, — и проходила всё время, пока ошибка жила.
  Теперь меню открывается не с верха страницы, фон крутится по-настоящему,
  меряется зазор между низом шапки и верхом панели и проверяется, что
  после закрытия страница на том же месте.

  Раздел виден из двух мест: блок «География» на главной (`#goroda` — три
  карточки городов и блок про остальные регионы) и строка в подвале.
  **Из главного меню «Города» убраны — их место заняла «Обо мне»**, по решению
  заказчика. Довод: весь сайт построен на «проверьте меня», и страница
  с обязательствами нужна человеку ровно перед тем, как отдать устав
  и заплатить, а до перестановки её можно было найти только на 17-м экране
  главной, в подвале и на контактах. Цена названа вслух и записана здесь:
  городские страницы потеряли ссылку со всех 52 страниц разом. Седьмым
  пунктом «Обо мне» добавить было нельзя — замер в `Header.astro`.
  Шестой пункт меню поднял порог навигации с 1000 до 1100 px, а полосу,
  где прячется подпись под телефоном, — с 1179 до 1279: на 1180 шапка
  с подписью давала 37 px горизонтальной прокрутки. Города везде идут
  **по алфавиту**: любой другой порядок пришлось бы обосновывать рейтингом,
  которого у меня нет. Title и description городской страницы уложены
  в 70 и 200 знаков с запасом (максимум 61 и 197) — считал по всем городам
  сразу, длиннее всех выходят Санкт-Петербург, Нижний Новгород и Сургут
  («Ханты-Мансийский автономный округ — Югра» в одном только субъекте).

  **Длина главной меряется, а не оценивается на глаз.** На телефоне она была
  27 экранов, на компьютере 13, до формы заявки 22,6 экрана прокрутки.
  Подрезано то, что страница говорила дважды: таблица «кому нужна СРО»
  перешла в короткий вид (`ComparisonTable brief` — формулировки со статьями
  без пояснений, зато все одиннадцать случаев вместо обрезанных восьми),
  разбивка взносов — из двух почти одинаковых карточек в одну таблицу,
  итог по срокам потерял то, что уже сказано шагом 3 и вопросом в FAQ,
  а блок «остальные регионы» — фразу, слово в слово повторявшую подзаголовок
  своей же секции. Стало 26,0 и 12,5 экрана, до формы 21,5.
  **Вывод, который важнее самих цифр: длина главной — не вода.** Оценка
  «ужмём до 18 экранов» оказалась неверной, и её проверили работой: после
  вычистки всех повторов ушло 959 px из 22 926. Остальное — калькулятор,
  предложение, разбивка цен, порядок работы, реестры, география, доверие,
  услуги, вопросы и форма; ни один из этих блоков не лишний, и резать
  дальше означало бы резать по содержанию. Если к этому вернутся, начинать
  надо с вопроса «какой раздел убираем целиком», а не с подрезки абзацев.

  **Тёмная шапка `.page-head` красится ОБЩИМИ правилами** в `global.css`:
  фон, цвет заголовка, подзаголовка, выделения жирным и крошек. Раньше
  каждая страница объявляла этот набор у себя в scoped-стилях, и первая же
  новая страница получила прозрачную шапку: светлый текст лёг на белую
  бумагу (2.13:1), а `<b>` внутри подзаголовка красился чернилами по бумаге —
  чёрным по чёрному, 1:1, буквально невидимо. Проверка контраста это поймала,
  но из разметки причину было не увидеть: класс тот же самый. Страницы вправе
  доопределять свои отступы, но фон и цвета трогать больше не нужно.

  **`scripts/test-hero-photo.mjs` теперь сам ищет неучтённые фотографии.**
  Список шапок в нём написан руками нарочно, но «падать, если забыли вписать»
  он умел только в теории: искать было некому. Теперь обходит сборку
  и требует пункт в списке для каждой страницы со слоем `.head-photo`.
  Первым же запуском нашёл страницу 404 — фотография у неё была с самого
  начала, а читаемость текста поверх кадра не мерил никто.

    **`llms.txt` и `llms-full.txt`** — оглавление и полные тексты статей
  для ИИ-помощников. Собираются из тех же конфигов, что и страницы;
  ни одного факта, написанного там руками, быть не должно — иначе заводится
  вторая точка правды, и расхождение раздаётся помощникам разом.

    **Поисковая часть — `src/config/seo.ts` и `scripts/check-seo.mjs`.**
  Отдельно от `site.ts`: там контакты, которые правят часто, здесь коды
  и ключи, которые вписывают один раз при запуске на боевом домене.
  Пустая строка означает «не подключено» и тег не выводится вовсе:
  пустой `content` у `yandex-verification` Вебмастер считает неверным кодом.
  В `Base.astro` микроразметка собрана графом: `ProfessionalService` (#org),
  `WebSite` (#website), `WebPage` (#webpage у каждой страницы) плюс крошки,
  услуги, статьи и вопросы. Узлы связаны через `@id`, и проверка требует,
  чтобы каждая ссылка разрешалась — в том числе на узел, объявленный
  вложенным (знак организации живёт внутри узла организации).
  `lastmod` в карте сайта: у статей — из их собственного `updated`, того же,
  что стоит под штампом «Сверено»; у остальных страниц — из истории git
  (поэтому у `actions/checkout` стоит `fetch-depth: 0`). Времени сборки там
  быть не должно: это означало бы «на каждой публикации изменились все
  двадцать страниц разом», и за такое `lastmod` перестают учитывать вовсе.
  Значки сайта растровые (`scripts/make-icons.mjs`) — SVG браузеры понимают,
  а значок в выдаче Яндекса и иконка на телефоне берутся из PNG; квадратные
  варианты без скруглений нарочно, iOS скругляет сам и уголки уже
  скруглённого файла дорисовывает чёрным.
  **Разметки отзывов на сайте нет и быть не должно**, пока отзывы не появятся
  настоящие: `aggregateRating` рисуется одной строкой, а снимается вместе
  со всем расширенным видом сайта. Это то же правило, по которому здесь нет
  выдуманных цифр в тексте.

  **Полоса про cookie — `CookieBanner.astro`, и она же единственный
  выключатель Метрики.** Счётчик подключается ИЗ её скрипта после согласия,
  а не стоит в разметке: до нажатия кнопки к `mc.yandex.ru` не уходит
  ни одного запроса (проверено перехватом запросов: 0 → 1 после «Принять»,
  0 после «Только необходимые» и при повторном заходе). Оттуда же убраны
  `preconnect` к Яндексу и картинка-пиксель в `noscript` — оба срабатывали бы
  до всякого выбора. **Полоса показывается, только когда `metrikaId`
  заполнен**: без счётчика сайт не ставит ни одного файла cookie, и полоса
  «сайт использует cookie» была бы ровно тем враньём мелким шрифтом, против
  которого здесь всё построено. Выбор хранится в `localStorage`, а не в cookie:
  хранить согласие на cookie в cookie до получения согласия — замкнутый круг.
  Высота нижней панели связи **меряется скриптом**: записанные по отступу
  `body` 58 px оказались 61, и полоса накрывала кнопку «Позвонить» на 3 px.
  **Проверки открывают браузер через `scripts/lib/browser.mjs`** — обёртку
  над playwright, которая отвечает за посетителя «только необходимые» ещё
  до загрузки страницы. Без этого полоса перехватывала клики у нижнего края
  и роняла прогон через раз, причём каждый раз другой скрипт: «✗ Проверка
  не прошла: калькулятор взносов», при том что тот же скрипт по отдельности
  проходил. `test-contrast.mjs` нарочно берёт настоящий playwright — иначе
  единственная проверка, которая меряет саму полосу, перестала бы её видеть.

  **Калькуляторы связаны с заявкой** — `src/config/leadfields.ts`.
  Оба калькулятора кладут снимок ответов в `sessionStorage` под ключом
  `CALC_KEY`, форма его подхватывает: подставляет вид работ и прикладывает
  разбор к письму. Три правила, которые нельзя сломать: снимок показывается
  **видимой строкой** с кнопкой «Убрать» (скрытое поле, уходящее вместе
  с заявкой втайне от отправителя, — ровно то, за что сайт ругает
  конкурентов); снимок живёт `CALC_TTL` = 30 минут (назавтра человек
  не помнит, что отвечал); идентификаторы видов работ в `leadfields.ts`
  те же, что в `Calculator.astro`, иначе подстановка промахивается молча.
  Свежий расчёт перебивает черновик, но не перебивает выбор, сделанный
  человеком ПОЗЖЕ расчёта, — иначе полоса сообщала «Проектирование»,
  а список показывал «Строительство».
  Квиз с гейтом («шесть вопросов → узнать стоимость → телефон») здесь
  сделан НЕ был и делаться не должен: цена на сайте открыта, и прятать
  за телефон число, которое страница «Стоимость» показывает бесплатно,
  значит убить главный довод сайта.
  Стережёт `scripts/test-form.mjs`: весь путь от калькулятора до письма,
  совпадение видимого текста со скрытым полем, срок годности снимка
  и совпадение списков. Проверено двумя подделками. Обход табом там идёт
  **до кнопки**, а не фиксированные восемь нажатий: записанное число молча
  превратило проверку достижимости в проверку длины формы.

  **Боевая сборка — `npm run release -- домен.ru`** (`scripts/release.mjs`),
  и от превью она отличается пятью значениями, каждое из которых можно забыть
  МОЛЧА: `SITE_URL`, `BASE_PATH`, `PUBLIC_NOINDEX`, `PUBLIC_LEAD_ENDPOINT`
  и домен в `submit.php`. Сайт при этом соберётся и будет выглядеть рабочим —
  с адресом example.ru в карте сайта, с запретом индексации или с формой,
  которая не отправляет заявки. Поэтому скрипт задаёт всё сам из одного
  значения (домена), а потом ПРОВЕРЯЕТ собранное: ищет следы превью, смотрит
  robots.txt, считает страницы с noindex (ожидаются ровно две — 404 и поиск,
  список написан руками: собранный из исходников пропустил бы протёкший
  `PUBLIC_NOINDEX`, потому что «ожидаемым» стало бы ровно то, что собралось),
  и только потом пакует архив. Домен в репозитории не хранится: `submit.php`
  правится уже в `dist`, иначе при смене домена он остался бы в двух местах.
  Токен формы берётся ИЗ `submit.php` — у него одна точка правды, и разметка
  с обработчиком рассогласоваться не могут.

  **`npm run test:lead`** (`scripts/test-lead.mjs`) — единственная проверка,
  которая трогает PHP. Остальные гоняются на превью, где PHP нет вовсе:
  форма там честно показывает телефон вместо отправки, и весь обработчик
  остаётся непроверенным до самого хостинга. Здесь поднимается настоящий
  `php -S` на боевой сборке, браузер заполняет настоящую форму, письмо
  пишется в файл (`NORMA_MAIL_DRY_RUN`). Проверяется, что в письме есть
  всё введённое, что отправитель — ящик на домене сайта (иначе SPF
  не сойдётся и письма молча уйдут в спам), что есть `Reply-To`, что заявка
  легла в журнал, что журнал не отдаётся по прямой ссылке и что заявку
  с чужого Origin обработчик не принимает. `ALLOWED_HOST` подменяется
  на время проверки и возвращается через `process.on('exit')` — иначе
  упавшая проверка оставила бы в архиве домен 127.0.0.1.

  **`public/.htaccess`** едет в сборку сам (Astro копирует `public/`).
  Делает четыре вещи, которых иначе не будет: своя страница 404 вместо
  заглушки хостинга, один канонический адрес (http→https и www→без www,
  иначе сайт открывается по четырём адресам и поисковик делит между ними
  вес), сжатие и кеширование. Кеш раздаётся по правилу «файл с хешем
  в имени — навсегда, файл без хеша — ненадолго»: `_astro/*.css|js`
  на год с `immutable`, шрифты и картинки на месяц без него (имя при
  подмене не меняется), HTML — `no-cache`, индекс поиска и карта сайта —
  час. Редирект на https проверяет ДВА признака (`HTTPS` и
  `X-Forwarded-Proto`): на shared-хостинге SSL снимает nginx, и до Apache
  переменная `HTTPS` может не дойти — правило без второго условия
  отправило бы браузер по кругу. Пути в файле от корня домена, поэтому
  он верен только когда сайт лежит в корне.
  **`/.well-known/` выведен из-под перенаправления**: Let's Encrypt
  подтверждает владение доменом запросом по HTTP и делает это не только
  при выпуске, но и при каждом продлении — каждые 90 дней и молча.
  Без исключения продление однажды не прошло бы, сертификат истёк бы,
  и сайт лёг бы без единой правки.
  **Сертификат выпускается ДО заливки файлов**, и это записано и в README,
  и в подсказке `release.mjs`: залитый до сертификата `.htaccess` отправляет
  всех на https, которого ещё нет, и сайт не открывается совсем — файлы
  на месте, а браузер показывает ошибку сертификата. В первой версии
  подсказки шаги стояли наоборот (сначала залить, потом SSL) — ровно
  та ловушка, которую скрипт и должен был снимать.

  **Архивы и дампы закрыты от скачивания** (`.zip`, `.tar`, `.gz`, `.sql`,
  `.bak` и прочие): сайт заливается архивом, архив остаётся в `public_html`,
  пока его не удалят, — и качается по прямой ссылке вместе с `api/submit.php`,
  то есть с почтовым ящиком владельца и всем исходником обработчика заявок.
  Распаковке правило не мешает, закрыт только доступ из браузера. Удалять
  архив после распаковки всё равно надо — это второй рубеж, а не замена.

  **Домен — `norma-sro.ru`**, куплен 25 сентября 2026 на Timeweb (оплачен
  до 25 сентября 2027, автопродление включено), DNS делегирован: домен
  и `www` отвечают адресом сервера Timeweb. Открыть сайт по домену
  из среды сборки нельзя — сетевая политика отдаёт 403 на сам домен,
  так что проверки «как оно снаружи» остаются за заказчиком по таблице
  из README.

  **Приём заявок — `public/api/submit.php`.** Журнал заявок называется
  `leads.log.php` и первой строкой содержит команду PHP «остановись»:
  в нём персональные данные, а `.htaccess` действует только на Apache —
  на nginx-хостинге файл `leads.jsonl` открывался бы по прямой ссылке.
  **В комментариях этого файла закрывающего тега PHP быть не должно**:
  однострочный комментарий обрывается не только переносом строки, но и им,
  и всё, что после, сервер отдаёт посетителю как текст — на этом уже
  попались, кусок исходника ушёл в ответ на заявку. Объявление `fail()`
  идёт без типа `never`: он появился в PHP 8.1, а на хостинге с 8.0 файл
  не выполнился бы вовсе. Проверяется запуском: `php -S` плюс
  `NORMA_MAIL_DRY_RUN` — письмо пишется в файл вместо отправки.

  **Политика обработки данных не должна выглядеть черновиком.** На странице
  `politika.astro` стоял видимый блок «это рабочая редакция, покажите
  юристу» — он писался владельцу, а читал его посетитель. Просьбы перенесены
  в комментарий и в README: показать текст юристу, дописать почтовый адрес
  оператора, подать уведомление об обработке персональных данных в РКН.

  **`npm run facts` напоминает про обещанные фотографии городов.**
  Снимок в шапке показывается, только если файл лежит в `public/img/`, —
  так заказчик кладёт его без правки кода. Обратная сторона: имя, вписанное
  в `regions.ts` под файл, которого ещё нет, ничего не ломает и теряется
  навсегда. Москва, Петербург и Ростов свои снимки получили; напоминание
  осталось для следующих городов.

  **Карты России на сайте нет — убрана по решению заказчика.** Она стояла
  в разделе «География» и была настоящей: границы всех субъектов из открытого
  набора данных, проекция Альберса, сетка меридианов и параллелей, наведение
  с показаниями по субъекту. Смысл она имела при тридцати трёх городах —
  показывала, что субъектов много и «рядом» не значит «тот же субъект».
  С тремя городами довод исчерпался: карта страны ради трёх меток занимала
  пол-экрана и говорила меньше, чем три карточки под ней.
  Удалены `GeoMap.astro`, `config/mapdata.ts`, `scripts/build-map.py`,
  `scripts/test-geomap.mjs` и поля `lon`/`lat` в `regions.ts` — они жили
  только ради карты, и оставлять данные без потребителя здесь нельзя.
  Всё лежит в истории git, коммит «Карта убрана».

  **Числа, написанные словами, обязаны считаться.** «Двадцать четыре
  разбора» на странице 404 разошлись с сайтом, когда статей стало
  двадцать шесть: такую ошибку не ловит ни одна проверка — она
  не противоречит ничему, кроме действительности. Счётчики на `404.astro`
  и `poisk.astro` берутся из коллекции.

  **У обоих workflow стоит `fetch-depth: 0`.** Карта сайта берёт даты
  из истории git, и на мелкой копии их нет. У робота-наблюдателя это
  давало ложную тревогу «собственные проверки не прошли» каждую неделю —
  а еженедельная ложная тревога хуже отсутствия тревоги. Заодно
  `check-seo.mjs` теперь различает: у статьи дата берётся из `updated`
  и её отсутствие — ошибка, у обычной страницы из git — замечание.

  **`npm test` поднимает сайт сам** (`scripts/run-tests.mjs`): раньше
  проверки молча требовали открытого предпросмотра на 4321 и без него
  выдавали `ERR_CONNECTION_REFUSED`, по которому не догадаться, что дело
  не в сайте. Если на порту уже кто-то отвечает — берётся он.

    Проверки стерегут решения: `scripts/test-contrast.mjs` — контраст всех 3047
  надписей на 23 страницах (он же поймал штамп «Проверено» на шапке, которая
  завела своё имя класса вместо `.page-head`); `scripts/check-links.mjs` —
  ссылки, мета-теги, уникальность якорей и слипшиеся слова; `npm test` —
  калькуляторы, форма, доступность и вёрстка на всех ширинах.
  Инструкция для заказчика — `norma-site/README.md`.
- **`pto-site/`** — сайт X-PTO: документальное сопровождение строительства
  (ИП Яковлев Андрей Эдуардович, Тверь; работа удалённая по всей России).
  Статика без сборщиков: генератор `build.py` на голой стандартной библиотеке
  Python подставляет данные из `data/` в `templates/base.html`. Двадцать одна
  страница, из них четырнадцать — страницы услуг. Публикуется на GitHub Pages
  шагами внутри `.github/workflows/deploy-orders-app.yml`, проверки —
  `.github/workflows/check-pto-site.yml`.

  Единые точки правды: `data/site.json` — контакты, реквизиты, блоки главной,
  коды для поисковиков (`seo`); `data/services.json` — услуги; `data/legal.json` —
  политика обработки персональных данных и текст согласия. Реквизиты в юридических
  текстах не пишутся руками, а подставляются из `site.json` через `legal_tokens()`:
  иначе редакции неизбежно разойдутся.

  **Персональные данные.** Форма собирает имя и телефон, поэтому на сайте есть
  страница `/politika/` (политика по 152-ФЗ плюс текст согласия), ссылка на неё
  в подвале каждой страницы и обязательная, заранее НЕ проставленная отметка
  согласия в форме — заранее проставленная согласием не считается. Ответственное
  лицо (ст. 22.1) — Яковлев Андрей Эдуардович. `tests/check_static.py` падает,
  если политика пропала, если со страницы исчезла ссылка на неё или если галочку
  проставили заранее. Почтовый адрес оператора в `legal.json` → `address` пока
  пуст: пустое поле НЕ выводит строку на страницу (документ не должен выглядеть
  черновиком), а сборка о нём напоминает. Уведомление в Роскомнадзор — отдельная
  обязанность владельца, кодом не закрывается.

  **Cookie и Метрика.** Счётчик подключается ИЗ скрипта полосы согласия, а не
  стоит в разметке: до нажатия «Принять» к Яндексу не уходит ни одного запроса
  (проверено перехватом: 0 → 1). Полоса и раздел 15 политики про cookie
  появляются, только когда заполнен `site.json` → `seo.metrika_id`. Пока его нет,
  сайт не ставит ни одного файла cookie, и полоса «сайт использует cookie» была бы
  враньём. То же правило, что в `norma-site`.

  **Оформление** — тёмно-синий `#0d1c30`, акцент `#1f6feb`, чертёжная сетка
  на тёмных блоках. Три шрифта, все урезаны до нужных символов и лежат на своём
  сервере: Geologica — заголовки, Inter — текст, JetBrains Mono — цифры и подписи.
  Урезанные наборы стережёт `tests/check_fonts.py`: символ вне набора браузер молча
  заменяет системным шрифтом, и на глаз это замечают не сразу.
  **Блок тёмной темы обязан идти последним в `assets/style.css`.**

  **Цвета подбираются по худшему фону, а не по самому светлому.** `--ink-soft`
  дважды затемняли: сначала с `#7286a0` (3,7:1 на белом), потом с `#5f7288` —
  он давал нужные 4,9:1 на белом, но 4,42:1 на серой плашке `--surface-alt`,
  а мелкие подписи стоят и там, и там.

  **Шапка на телефоне липкая, но уезжает при прокрутке вниз.** В медиа-запросе
  стояло `.header { position: relative }` — оно понадобилось, чтобы выпадающая
  панель меню привязывалась к шапке, и заодно молча отменяло липкость: до меню
  и до кнопки звонка приходилось мотать страницу до самого верха. Привязка
  работает и без него: `sticky` — тоже позиционирование. Взамен шапка прячется
  при движении вниз и возвращается при движении вверх (`transform`, не высота:
  высота пересчитывала бы раскладку каждый кадр). Порог 61.1875em тот же,
  что у переключения меню в бургер, — **эти два правила двигаются только вместе**.
  Прокрутку фона под открытым меню держит фиксация `body` на запомненном
  смещении плюс `position: fixed` у шапки: одного `overflow: hidden` на `body`
  телефон не слушает, а сам он ломает `sticky`. Возврат прокрутки —
  `behavior: 'instant'`, иначе страница на глазах мотается назад.
  Стережёт `tests/check_browser.py`: он меряет поведение, а не наличие класса,
  и нажимает по координатам, потому что обычный `click()` сам прокручивает
  страницу и подменяет то, что меряется.

  **Подпись под «X-PTO» в шапке видна на всех ширинах, кроме полосы
  979–1344 px** — там в строку шапки не помещается меню целиком. На телефоне
  она переносится на две строки (просьба заказчика: одно «X-PTO» человеку
  по ссылке ничего не объясняет). Границы полосы стыкуются с мобильным
  блоком без зазора: был зазор в 2 px (правила кончались на 979 и начинались
  с 981), и на 980 px шапка вылезала за край на 97 px; сверху граница стояла
  на 1319 px, и до 1336 px вылезала ещё на 1–9 px. Обе щели нашёл только
  перебор всех ширин с шагом 4 px — он теперь в `tests/check_browser.py`.

  **`config.js` в сборку не попадает.** Раньше сборка клала в `dist/`
  заглушку «чтобы не было 404», и при обновлении сайта она затирала на хостинге
  настоящий файл с настройками заявок — форма молча переставала отправлять.
  `check_browser.py` не считает 404 на `config.js` ошибкой — только на нём.

  **Хостинг — Timeweb, домен x-pto.ru** (`base_url`). **Заявки приходят письмом,
  а не в Telegram** — решение заказчика. `server/api/zayavka.php` (сборка кладёт
  его в `dist/api/`, вписывая адрес из `contacts.email`: одна точка правды)
  отправляет `mail()` от `noreply@<домен>`. Необязательный `zayavki-config.php`
  выше `public_html` (в git не попадает) меняет адрес или включает `'dry_run'` —
  тогда письмо не уходит, а текст возвращается в ответе: так весь путь
  проверяется через `php -S`. Заявка на сервере не сохраняется, на диске только
  хеш адреса для ограничения «пять заявок за десять минут». С уходом от Telegram
  из политики и согласия убрана трансграничная передача (редакция 1.2):
  вернёте Telegram — верните и её.

  **Переход на https в `server/.htaccess` включён** (сертификат Let's Encrypt
  на x-pto.ru, 25.09.2026). Проверка https двойная — `%{HTTPS}` и
  `X-Forwarded-Proto`: у Timeweb перед Apache стоит nginx, и по одному
  `%{HTTPS}` сайт ушёл бы в бесконечную переадресацию. www снимается первым
  правилом сразу на https — на www сертификата может не быть.

  **«89 субъектов РФ» на первом экране** — по действующей Конституции.
  Стояло 85; на аудите перед запуском заказчик сначала сказал оставить,
  затем поправился: 89.

  **Числа словами считаются по данным** (`number_word()`): «Тринадцать
  направлений» на странице услуг простояло, когда их было четырнадцать.

  **Числа, которые можно померить, меряются.** Высота нижней панели связи
  записана в `--call-bar-h` скриптом: записанные в отступе `body` 66 px на деле
  оказались 63, и под панелью стоят и отступ страницы, и полоса про cookie.
  `lastmod` в карте сайта берётся из истории git по исходнику страницы: дата
  сборки означала бы «на каждой публикации изменились все страницы разом»,
  и за такой `lastmod` поисковики перестают его учитывать.

  **Обложки для соцсетей** рисует `tools/make-og.py` — по одной на страницу,
  браузером, теми же цветами и шрифтами, что и сайт. Заголовок берётся
  из готовой страницы (`<h1>`), а не пишется в инструменте руками.
  Порядок: `build.py` → `make-og.py` → `build.py`.

  Инструкция для заказчика — `pto-site/README.md`.
- **`.claude/skills/construction-control/`** — скилл строительного контроля (ниже).

---

**`/construction-control`** — кастомный Claude Code skill,
ИИ-ассистент строительного контроля: по документам стройки (договор, смета/ВОР,
график, планы помещений, техкарты) и видеообходам объекта он оценивает прогресс,
сравнивает факт с графиком/сметой, находит отклонения, дефекты и риски и формирует
недельный отчёт.

## Где что лежит

- `.claude/skills/construction-control/SKILL.md` — точка входа скилла (принципы,
  5 агентов, оркестрация, рабочий процесс из 13 шагов).
- `.claude/skills/construction-control/references/` — архитектура, модель данных,
  логика агентов, MCP-инструменты, обработка видео, расчёт отклонений,
  human-in-the-loop, workflow, MVP, критерии приёмки, тесты, риски.
- `.claude/skills/construction-control/schemas/` — строгие JSON Schema всех выходов.
- `.claude/skills/construction-control/scripts/` — `deviation.py` (расчёт отклонений),
  `validate.py` (валидация JSON по схемам).
- `.claude/skills/construction-control/templates/weekly-report.md` — шаблон отчёта.

## Как активируется скилл

В сессии Claude Code, открытой на этом репозитории, скилл подхватывается
автоматически. Запуск: пользователь загружает документы/видео и пишет
`/construction-control` либо просто описывает задачу (например: «оцени прогресс по
этому обходу и графику, найди отставания»).

## Незыблемые правила скилла (соблюдать при любой работе в этом репозитории)

- Факт о выполненных работах — только по кадрам-доказательствам (`EvidenceFrame`);
  ссылки на документы — только реальные, иначе `null` и ниже `confidence`.
- Все выходы агентов — строго по JSON Schema из `schemas/` (проверять `validate.py`).
- Расчёты отклонений — детерминированной арифметикой (`deviation.py`), не «на глаз».
- Human-in-the-loop: система НЕ подписывает акты, НЕ подтверждает скрытые работы по
  видео, НЕ принимает работы окончательно; критические/спорные выводы помечаются
  `requires_human_review: true`.

## Статус реализации

Скилл — это полный **проект и оркестрация**: методология, промпты агентов, схемы
данных и рабочие расчётные скрипты. Продовый backend (MCP-сервер `construction-mcp`
с реальными парсерами PDF/DOCX/XLSX, извлечением кадров, OCR/ASR, БД и векторным
поиском) описан в `references/mcp-tools.md`, но как отдельная система ещё не собран.
До его реализации скилл работает в «ассистируемом» режиме: Claude сам читает
загруженные файлы и кадры и следует методологии скилла.

## Быстрая проверка

```bash
cd .claude/skills/construction-control
python3 scripts/deviation.py --demo            # пример расчёта отклонения
pip install jsonschema
python3 scripts/validate.py issue <файл.json>  # валидация проблемы по схеме
```
