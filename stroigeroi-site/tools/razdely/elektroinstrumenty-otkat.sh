# Откат elektroinstrumenty.sh: возвращает прежние название и заголовок из oc_rename_before.
( cd /var/www/u2934771/data/www/stroigeroi.ru || exit 1; php -r 'mysqli_report(MYSQLI_REPORT_OFF); include "config.php"; $m = @new mysqli(DB_HOSTNAME, DB_USERNAME, DB_PASSWORD, DB_DATABASE, (int)DB_PORT); if ($m->connect_error) { echo "DB: ", $m->connect_error, "\n"; exit(1); } $m->set_charset("utf8"); $p = DB_PREFIX; if (!$m->query("SELECT 1 FROM {$p}rename_before LIMIT 1")) { echo "STOP: no {$p}rename_before - nothing to undo\n"; exit(1); } $ok = $m->query("UPDATE {$p}category_description d JOIN {$p}rename_before b ON b.category_id = d.category_id AND b.language_id = d.language_id SET d.name = b.name, d.meta_title = b.meta_title") && $m->query("DROP TABLE {$p}rename_before"); echo $ok ? "undone\n" : "SQL: " . $m->error . "\n";' )
