// ЕДИНАЯ ТОЧКА ПРАВДЫ ДЛЯ ВСЕГО САЙТА.
// Телефон, почта, мессенджеры, реквизиты и счётчик Метрики меняются ТОЛЬКО здесь —
// весь сайт (шапка, подвал, контакты, формы, микроразметка) обновится сам.

export const SITE = {
  // Название сайта (бренд) и подпись под ним.
  brand: 'НОРМА',
  brandTag: 'вступление в СРО',

  // Юридические данные — показываются в подвале, контактах и политике ПДн.
  legalName: 'ИП Багишев Алихан Ахадович',
  shortName: 'Алихан Багишев',
  inn: '470418675847',
  // Ваша фотография для блока консультации на странице контактов.
  // Файл лежит в norma-site/public/img/, здесь только имя — так же,
  // как heroPhoto ниже. Показывается квадратом 96 px и целиком, без обрезки,
  // поэтому файл нужен квадратный и вдвое крупнее (от 192×192): на экранах
  // с двойной плотностью точек фотография в 96 px выглядит мыльной.
  // Если строку опустошить, вместо фотографии вернётся фирменный знак —
  // это лучше, чем чужое стоковое лицо: на сайте, который обещает работу
  // лично, стоковый человек в этом блоке был бы первой же неправдой.
  photo: 'foto.webp',
  // ОГРНИП. Строка сама показывается в подвале, в реквизитах на контактах
  // и в политике обработки данных. Контрольная цифра сходится, структура
  // читается: 3 — индивидуальный предприниматель, 26 — год записи,
  // 78 — Санкт-Петербург.
  ogrnip: '326784700317132',

  // Контакты.
  phone: '+7 931 969-86-64',
  phoneHref: 'tel:+79319698664',
  email: 'bagishevdelo@inbox.ru',
  whatsapp: 'https://wa.me/79319698664',
  telegram: 'https://t.me/+79319698664',
  // MAX не умеет ссылку по номеру телефона. Личная ссылка вида https://max.ru/u/…
  // берётся в приложении MAX («Поделиться» / QR-код). Впишите её — кнопка MAX
  // сама появится в контактах и подвале.
  maxLink: 'https://max.ru/u/f9LHodD0cOIew4NCbh1RQfZrPkUgB7MfWbADwjC6bMlFEQKryZyEfRUZgIc',

  // География. Работа дистанционная, поэтому упор на охват, а не на города:
  // клиенту из Иркутска название чужого города ничего не даёт, а «по всей
  // России» отвечает на его вопрос сразу.
  geo: 'Работаю по всей России · выписка из реестра за 2–3 дня',
  geoShort: 'по всей России · выписка за 2–3 дня',

  // Фотография на фон первого экрана. Пусто — фон рисуется чертежом
  // (сетка миллиметровки и контур дома с краном), как сейчас.
  //
  // Как поставить свою: положите файл в public/img/ и впишите сюда его имя,
  // например 'hero.jpg'. Больше ничего править не нужно — обработка,
  // затемнение и растворение в белую бумагу включаются сами.
  //
  // Какая фотография подойдёт, а какая испортит первый экран — в README,
  // раздел «Фотография на первом экране».
  heroPhoto: 'hero.webp',

  // Номер счётчика Яндекс.Метрики, например '99999999'.
  // Пока пусто — счётчик не подключён, код на сайт не вставляется.
  metrikaId: '',

  // Дата последней сверки норм и сумм на сайте (штампы «Проверено»).
  // Меняется при каждой проверке содержимого.
  checkedDate: '29 августа 2026',
  checkedDateISO: '2026-08-29',
} as const

// Официальные реестры для блока проверяемости.
export const REGISTRIES = {
  nostroy: 'https://reestr.nostroy.ru',
  nopriz: 'https://nopriz.ru',
  /** Проверка статуса ИП по ИНН — сервис ФНС «Прозрачный бизнес» / ЕГРИП. */
  egrip: 'https://egrul.nalog.ru/',
} as const

// Ссылка внутри сайта с учётом подпапки (на GitHub Pages сайт живёт
// в /репозиторий/norma/, на своём домене — в корне).
export const href = (path: string): string =>
  import.meta.env.BASE_URL.replace(/\/$/, '') + path
