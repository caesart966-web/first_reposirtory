// НАСТРОЙКИ ДЛЯ ПОИСКОВЫХ СИСТЕМ.
//
// Отдельно от site.ts нарочно: там контакты и реквизиты, которые заказчик
// правит руками и часто, здесь — коды и ключи, которые вписываются один раз
// при запуске на боевом домене. Пустая строка означает «ещё не подключено»:
// тег не выводится вовсе. Это важнее, чем кажется: пустой
// <meta name="yandex-verification" content=""> Вебмастер считает неверным
// кодом и права по нему не подтверждает.

export const SEO = {
  /**
   * Код подтверждения прав в Яндекс.Вебмастере.
   *
   * Где взять: webmaster.yandex.ru → добавить сайт → способ «Мета-тег».
   * Яндекс покажет строку вида <meta name="yandex-verification" content="a1b2c3d4e5f6" />.
   * Сюда вписывается ТОЛЬКО значение content, без самого тега: 'a1b2c3d4e5f6'.
   */
  yandexVerification: '',

  /**
   * Код подтверждения прав в Google Search Console.
   *
   * Где взять: search.google.com/search-console → добавить ресурс
   * «Ресурс с префиксом в URL» → способ «HTML-тег». Из показанной строки
   * <meta name="google-site-verification" content="..."> сюда идёт только
   * значение content.
   */
  googleVerification: '',

  /**
   * Ключ IndexNow — протокол мгновенной переиндексации. Его понимают
   * Яндекс, Bing и Naver; Google официально не участвует.
   *
   * Ключ должен лежать на сайте отдельным файлом <ключ>.txt с этим же
   * содержимым — файл уже создан в public/. Меняете ключ здесь — переименуйте
   * и файл, иначе поисковик отвергнет отправку: он проверяет владение
   * доменом именно по нему.
   *
   * Отправка: npm run indexnow (после публикации на боевом домене).
   */
  indexNowKey: '6c695156c838e502cc958c309db6ec46',

  /**
   * Картинка-превью для мессенджеров и соцсетей: 1200×630.
   * Пересобирается командой npm run og, если поменялись название или телефон.
   */
  ogImage: '/og.png',
  ogImageAlt: 'НОРМА — вступление в СРО. Помощь строительным, проектным и изыскательским компаниям по всей России',
} as const
