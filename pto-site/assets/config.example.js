/* =========================================================================
   ОБРАЗЕЦ настроек приёма заявок.

   Что сделать:
   1. Скопируйте этот файл рядом и назовите его config.js
   2. Заполните ОДИН из двух вариантов ниже
   3. config.js не попадает в git (он в .gitignore) — заливайте его на хостинг руками

   Файл config.js обязателен: без него форма не отправит заявку.
   ========================================================================= */

window.SITE_CONFIG = {

  /* ---- Вариант А. РЕКОМЕНДУЕМЫЙ -----------------------------------------
     Адрес вашего обработчика (Cloudflare Worker из папки serverless/).
     Токен бота хранится на стороне обработчика и на сайт не попадает.
     Оставьте пустую строку, если пользуетесь вариантом Б.               */
  formEndpoint: 'https://zayavki.ваш-поддомен.workers.dev',

  /* ---- Вариант Б. БЕЗ СЕРВЕРА -------------------------------------------
     Прямая отправка в Telegram прямо из браузера.
     Работает на любом хостинге, но токен виден любому, кто откроет
     исходный код страницы. Когда это допустимо и как снизить риск —
     см. раздел «Заявки в Telegram» в README.
     Используется, только если formEndpoint выше пустой.                 */
  telegram: {
    botToken: '',   // например '7123456789:AAF...'
    chatId: ''      // например '-1001234567890' или '123456789'
  }
};
