<?php
/* =========================================================================
   НЕОБЯЗАТЕЛЬНЫЕ настройки приёма заявок для хостинга с PHP.

   Без этого файла заявки уходят на почту из data/site.json → contacts.email:
   сборка сама вписывает её в обработчик. Файл нужен, только если заявки
   надо получать на другой ящик, не показывая его на сайте.

   Что сделать:
   1. Скопируйте этот файл и назовите копию zayavki-config.php
   2. Впишите адрес
   3. Положите файл на хостинге ВЫШЕ публичной папки сайта — рядом
      с public_html, а НЕ внутрь неё. Тогда его нельзя открыть по адресу
      в браузере.

   В git и в сборку сайта этот файл не попадает никогда (он в .gitignore).
   ========================================================================= */

return array(
    'to' => '',          // куда присылать заявки, например 'zayavki@x-pto.ru'
    // 'dry_run' => true, // только для проверки: письмо не уходит, текст возвращается в ответе
);
