// Тематические изображения страницы (ЧАСТЬ 4 ТЗ). Держим их в одном месте:
// alt, размеры и пути нужны и в разметке, и в учёте лицензий
// (public/img/CREDITS.md), и расходиться они не должны.
//
// Все кадры — боевые. Источник и лицензия записаны у каждого в CREDITS.md;
// у slide-design URL источника заказчик пока не назвал, hero-day сгенерирован
// заказчиком в нейросети — это там же, отдельными строками.
//
// Слот legal (весы) снят 04.09.2026 вместе с блоком «Смежные юридические
// задачи»: заказчик убрал блок, а кадр жил только в нём. Файлы лежат
// в assets-src/legal-spare.*, лицензия записана в CREDITS.md — вернуть
// можно одной строкой.
//
export type PageImage = {
  src: string
  // Необязателен: у векторной графики второго формата нет и не нужно.
  srcAvif?: string
  alt: string
  width: number
  height: number
  ratio: string
  fit?: 'cover' | 'contain'
}

export const IMAGES = {
  // Три вида СРО. Один и тот же кадр стоит в слайдере первого экрана и в
  // шапке страницы своего вида — человек нажимает «Подробнее» на слайде и
  // узнаёт картинку на странице, куда пришёл.
  //
  // С 24.09.2026 кадры в натуральном цвете, без фирменного синего дуотона:
  // сайт перешёл на тёплую палитру, и синий кадр в ней выглядел чужим.
  // Кадры временные — заказчик генерирует серию из трёх в одном свете
  // («золотой час»); придут — заменить файлы, имена оставить.
  construction: {
    src: './img/hero-day.webp',
    srcAvif: './img/hero-day.avif',
    alt: 'Башенные краны и строящийся корпус со стеклянным фасадом в ясный день',
    width: 1672,
    height: 941,
    ratio: 'aspect-[16/9]',
  },
  design: {
    src: './img/slide-design.webp',
    srcAvif: './img/slide-design.avif',
    alt: 'Строительная каска, чертежи и кодексы на столе у окна с видом на стройку',
    width: 1721,
    height: 914,
    ratio: 'aspect-[16/9]',
  },
  survey: {
    src: './img/slide-survey.webp',
    srcAvif: './img/slide-survey.avif',
    alt: 'Геодезический прибор на штативе на площадке инженерных изысканий',
    width: 1200,
    height: 675,
    ratio: 'aspect-[16/9]',
  },
} satisfies Record<string, PageImage>
