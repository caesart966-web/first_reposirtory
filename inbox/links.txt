# 📎 Ссылки на большие файлы (больше 25 МБ)
#
# Вставьте ссылку на файл (по одной на строку) и нажмите Commit changes —
# файл будет скачан и сконвертирован в Markdown автоматически.
# Результат появится в папке converted/, сам файл в репозиторий НЕ попадает,
# поэтому размер практически не ограничен.
#
# Поддерживаются:
#   - прямые ссылки (https://.../file.pdf)
#   - Google Drive  (доступ: «все, у кого есть ссылка»)
#   - Яндекс.Диск   (публичная ссылка)
#   - Dropbox       (обычная ссылка «поделиться»)
#
# Строки, начинающиеся с #, игнорируются.
# Уже обработанные ссылки повторно не скачиваются
# (список хранится в converted/.links_done.json).

# Демо-ссылка для проверки работы (можно удалить):

