# Необязательные возможности. START.bat пытается их поставить,
# но если не получится — программа всё равно работает.
#
# docx2pdf     — сохранение готовых документов в PDF через установленный
#                на компьютере Microsoft Word (полностью локально).
#                Без него PDF можно сделать вручную: Файл → Сохранить как → PDF.
#                Также подойдёт установленный LibreOffice — программа найдёт его сама.
# tkinterdnd2  — перетаскивание файла карточки мышью в окно программы.
#                Без него работает кнопка «Выбрать файл…».

docx2pdf>=0.1.8; sys_platform == "win32"
tkinterdnd2>=0.3
