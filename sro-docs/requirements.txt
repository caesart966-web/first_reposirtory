# Основные библиотеки. Ставятся автоматически при первом запуске START.bat.
#
# lxml         — работа с внутренним XML документов Word (движок подстановки)
# python-docx  — чтение карточек компании в формате DOCX
# openpyxl     — чтение карточек компании в формате XLSX
# pypdf        — чтение карточек компании в формате PDF
#
# Интерфейс сделан на Tkinter — он входит в стандартную поставку Python
# для Windows, отдельно ставить ничего не нужно.

lxml>=4.9
python-docx>=1.1
openpyxl>=3.1
pypdf>=4.0
