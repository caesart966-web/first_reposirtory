# --- Обязательные зависимости -------------------------------------------------
pandas>=2.0            # табличная обработка + запасной ридер (sheet_name=None)
openpyxl>=3.1          # чтение/запись .xlsx / .xlsm, форматирование отчёта
xlrd>=2.0              # чтение устаревшего формата .xls
requests>=2.31         # HTTP-клиент для checko.ru
beautifulsoup4>=4.12   # разбор HTML карточки компании
lxml>=4.9              # быстрый парсер для BeautifulSoup
rarfile>=4.0           # распаковка .rar (нужен внешний unrar / bsdtar / 7z)
python-dateutil>=2.8   # разбор «человеческих» дат
tqdm>=4.66             # прогресс-бар

# --- Необязательные (ставятся по желанию) ------------------------------------
# pyxlsb>=1.0.10       # чтение бинарного формата .xlsb
