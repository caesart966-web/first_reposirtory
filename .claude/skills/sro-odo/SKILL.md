---
name: sro-odo
description: >-
  Обязательства члена СРО по договорам подряда — три режима. (1) Оценка
  одного договора: загружен договор подряда (с ДС, сметой, актами) — нужно
  сказать, требуется ли по нему членство в СРО и входит ли он в расчёт
  компенсационного фонда обеспечения договорных обязательств (КФ ОДО), с
  нормой закона под каждым выводом и под каждым видом работ. (2) Разбор
  письма компании, оспаривающей учёт договора: каждый довод проверяется
  юридическим тестом, выдаётся статус и проект ответа с нормами. (3) Реестр
  по пачке документов (договоры, сметы, КС-2/КС-3, УПД, уведомления в СРО):
  суммы раскладываются на «СРО» и «не СРО» по строительству, проектированию
  и изысканиям, отчёт проверяющему. Работает по Перечню 624 и ГрК РФ
  (ст. 1, 47, 48, 48.1, 51, 52, 55.8, 55.13, 55.16, 55.31), ГК РФ (ст. 421,
  431, 506, 702, 740, 753, 758, 779), с детерминированными скриптами,
  базой прецедентов и передачей спорного человеку. Ключевые слова: СРО, ОДО,
  КФ ОДО, совокупный размер обязательств, уровень ответственности, приказ 624,
  перечень видов работ, договор подряда, застройщик, технический заказчик,
  субподряд, порог 10 млн, конкурентные закупки, 309-ФЗ, КС-2, КС-3,
  уведомление в СРО, возражение, письмо в СРО, проверка члена СРО.
---

# Обязательства члена СРО (`/sro-odo`)

Три режима работы, общая правовая база и общая база прецедентов.

| Режим | Вход | Выход | Скрипт |
|---|---|---|---|
| **A. Оценка договора** | один договор с ДС, сметой, актами → карточка `schemas/contract-card.schema.json` | заключение: требуется ли членство, входит ли в ОДО, что из работ относится к СРО, с нормами под каждым шагом | `scripts/assess.py` |
| **B. Разбор письма** | письмо-возражение компании → `schemas/objection.schema.json` | оценка каждого довода (обоснован / частично / нет / нужны документы) с нормами и проект ответа | `scripts/objection.py` |
| **C. Реестр** | пачка документов компании → `schemas/register.schema.json` | суммы «СРО / не СРО» по видам СРО, отчёт проверяющему | `scripts/classify.py`, `odo.py`, `report.py` |

После любого режима решение человека записывается в базу прецедентов
(`scripts/cases.py`), и следующий похожий договор получает ссылку на него.
Как растут правила и каталог доводов — `references/learning.md`.

## Что делает и чего не делает

**Делает:**
- извлекает из документов договоры (стороны, предмет, цена, ДС, способ
  заключения), объекты и построчные суммы актов и смет — каждую с ссылкой
  на документ и страницу;
- относит договор к виду СРО и решает, входит ли он в совокупный размер
  обязательств (подрядчик ли компания, подряд ли это, кто заказчик);
- относит каждую строку к виду работ Перечня 624 и выводит: СРО / не СРО /
  решает человек;
- считает цену, выполненное по актам и остаток, и долю СРО в каждом —
  по договорам и по видам СРО, в двух выборках (все подходящие договоры
  и только конкурентные);
- выдаёт реестр JSON, сводку Markdown и отчёт HTML с очередью спорных
  вопросов человеку.

**Также делает (режимы A и B):** по одному договору выдаёт заключение с
нормой под каждым шагом; по письму-возражению — оценку каждого довода
и проект ответа; помнит решённые дела и показывает похожие.

**Не делает (см. `references/human-in-the-loop.md`):**
- не выносит вердикт «превышение»: уровень ответственности приводится
  справочно, сравнение с фактическим уровнем — по реестру СРО и решению
  человека;
- не назначает категорию объекта (ст. 48.1 ГрК РФ) и статус заказчика
  (застройщик, техзаказчик) сам — только по подтверждению;
- не переводит спорную строку в «СРО» без основания: сомнительное остаётся
  в «не решено» отдельной суммой;
- не додумывает цифры: скан не читается — `null` и пометка, а не догадка.

## Принципы

1. **Каждая сумма — из документа.** В реестре у строки есть `source`
   (документ, страница). Числа со сканов получают `extraction_confidence`
   не выше 0,8 и сверяются с итогами документа (`validate.py`).
2. **Классификация детерминирована.** `classify.py` применяет правила
   `scripts/data/rules.json` по Перечню 624; Claude заполняет реестр
   и разбирает строки, ушедшие на проверку, а не классифицирует «на глаз».
3. **Арифметика — скриптом.** `odo.py` считает суммы; вручную суммы не
   складываются и не переписываются.
4. **Два уровня, три вида.** Сначала договор (входит ли), потом строки
   (что внутри); строительство, проектирование и изыскания — отдельно.
5. **Отсутствие факта дешевле факта, за который нельзя ответить.**

## Рабочий процесс

### Режим A — один договор

1. Прочитать договор, ДС, приложения (смета, ВОР, график), акты, если есть.
   Сканы — постранично как изображения (`pymupdf`).
2. Заполнить карточку `card.json` по `schemas/contract-card.schema.json`.
   Каждое поле — с `basis`: пункт договора или документ. Неизвестное — `null`.
   Правила чтения — `references/contract-assessment.md`, «Что читает Claude».
3. **Чекпоинт A (человек):** статус заказчика, категория объекта по ст. 48.1,
   характер работ, способ заключения, цена с ДС. Показать таблицей, попросить
   подтвердить или поправить.
4. `python3 scripts/validate.py card.json --schema contract-card`
5. `python3 scripts/assess.py card.json -o assessment.json --md zaklyuchenie.md`
6. **Чекпоинт B (человек):** строки «решает человек» и раздел «Что нужно
   установить». Решения по строкам — в `works[].human_decision`, повторить шаг 5.
7. Отдать заключение. Спросить итоговое решение и записать прецедент:
   `python3 scripts/cases.py add --from-assessment assessment.json --decision "…" --note "…"`.

### Режим B — письмо-возражение

1. Прочитать письмо и приложения. Если по договору уже есть карточка и
   заключение — взять их; если нет — сначала режим A.
2. Заполнить `objection.json`: реквизиты, суть, доводы — `quote` дословно,
   `type` из каталога `scripts/data/objections.json` (нет подходящего —
   `other`), `facts` — только подтверждённые документами.
3. `python3 scripts/objection.py objection.json --card card.json --assessment assessment.json -o objection.json --md otvet.md`
4. **Чекпоинт (человек):** доводы со статусом «нужны факты» и «решает
   человек»; проект ответа — черновик для юриста.
5. Записать прецедент: `python3 scripts/cases.py add --from-objection objection.json --decision "…" --note "…"`.

### Режим C — реестр по пачке документов

Подробно — `references/workflow.md`. Кратко:

1. **Приём документов.** Сканы рендерятся постранично (`pymupdf`), читаются
   как изображения; электронные PDF — текстом. Одинаковые файлы (md5)
   отбрасываются, КС-3 и счета-фактуры — дубли КС-2 за тот же период.
2. **Реестр.** Заполняется `register.json` по `schemas/register.schema.json`:
   компания, объекты, договоры, документы, строки. Правила чтения каждого
   вида документа — `references/documents.md`.
3. **Чекпоинт A (человек):** роль компании в каждом договоре, статус
   заказчика, способ заключения, категория объекта по ст. 48.1, цена с ДС.
4. `python3 scripts/validate.py register.json` — схема, ссылки, суммы.
5. `python3 scripts/classify.py register.json` — разметка строк.
6. **Чекпоинт B (человек):** строки `review_status: pending`. Claude
   предлагает решение с основанием, человек подтверждает; решения пишутся
   в `classification` (`corrected`/`confirmed`, `human_note`).
7. `python3 scripts/odo.py register.json -o result.json --md summary.md` —
   расчёт и сводка.
8. `python3 scripts/report.py result.json -o report.html --register register.json` —
   отчёт проверяющему.
9. **Чекпоинт C:** отчёт помечен как информационный; выводы о соответствии
   уровню делает человек по выписке из реестра СРО.

## Файлы

| Путь | Что это |
|---|---|
| `references/law.md` | Правовая основа: три вида СРО, кто входит в расчёт, формула ч. 7 ст. 55.13, Перечень 624 и звёздочки, 309-ФЗ, что не сверено |
| `references/contract-assessment.md` | Режим A: что читать из договора, шесть шагов и их нормы, как читать вердикт |
| `references/objections.md` | Режим B: пятнадцать типовых доводов, тесты, статусы, проект ответа |
| `references/learning.md` | Как растут прецеденты, правила и каталог доводов; что нужно от человека на каждом договоре |
| `scripts/data/law.json` | Реестр норм: id → ссылка, суть, отметка о сверке; на него ссылаются все заключения |
| `../../../odo-app/` | Веб-оболочка для коллеги: загрузка документов, карточка, заключение, реестр, письма, прецеденты — на этом же движке |
| `scripts/data/policy.json` | Практика проверяющего и положение СРО: только конкурентные договоры в ОДО (44-ФЗ, 223-ФЗ, обязательные торги; добровольный тендер = прямой), без сопоставления с уровнем, субподряд с торгов и договоры ниже порога — не входят |
| `scripts/data/objections.json` | Каталог доводов с юридическими тестами и нормами |
| `schemas/contract-card.schema.json` | Карточка договора — вход режима A |
| `schemas/assessment.schema.json` | Заключение — выход режима A |
| `schemas/objection.schema.json` | Письмо и его разбор — режим B |
| `schemas/case.schema.json` | Прецедент |
| `scripts/assess.py` | Заключение по договору с нормами под каждым шагом и видом работ |
| `scripts/objection.py` | Разбор письма-возражения и проект ответа |
| `scripts/cases.py` | База прецедентов: add / find / list / stats / suggest |
| `scripts/test_assess.py` | 63 случая: договоры, строки, доводы |
| `cases/` | Анонимные прецеденты (`index.jsonl`, `case-*.json`) |
| `examples/contract-demo/` | Учебный договор, заключение, письмо и ответ (компания вымышлена) |
| `templates/assessment.md`, `templates/objection-reply.md` | Структура заключения и ответа |
| `references/classification.md` | Дерево решений по договору и по строке, особые случаи, как добавлять правила |
| `references/documents.md` | Как читать договор, ДС, смету, КС-2, КС-3, УПД, уведомление в СРО |
| `references/workflow.md` | Шаги и точки участия человека |
| `references/human-in-the-loop.md` | Что скилл никогда не решает сам |
| `schemas/register.schema.json` | Схема реестра — единственный вход и выход |
| `scripts/data/perechen-624.json` | Перечень 624 в машинном виде (I — изыскания, II — проектирование, III — строительство, звёздочки) |
| `scripts/data/rules.json` | Правила отнесения строк к Перечню и к «не СРО» |
| `scripts/data/levels.json` | Уровни ответственности и пороги (ст. 52, 55.16, 55.31 ГрК РФ) |
| `scripts/classify.py` | Классификатор строк (реестр или одна строка `--text`) |
| `scripts/odo.py` | Расчёт: применимость договора, доли, остатки, итоги, очередь на проверку |
| `scripts/report.py` | Отчёт HTML |
| `scripts/validate.py` | Валидация реестра по схеме, ссылкам и суммам |
| `scripts/test_rules.py` | Тест правил на строках из реальных актов |
| `templates/report.md` | Структура отчёта и сопроводительного письма |
| `examples/demo/` | Учебный пример (вымышленная компания) с результатами |

## Быстрая проверка

```bash
cd .claude/skills/sro-odo
python3 scripts/test_rules.py                       # правила: все случаи сходятся
python3 scripts/test_assess.py                      # договоры, строки, доводы
python3 scripts/assess.py examples/contract-demo/card.json --md /tmp/z.md --quiet
python3 scripts/objection.py examples/contract-demo/objection.json --card examples/contract-demo/card.json --md /tmp/o.md --quiet
python3 scripts/cases.py find examples/contract-demo/card.json
python3 scripts/validate.py examples/demo/register.json
python3 scripts/classify.py examples/demo/register.json
python3 scripts/odo.py examples/demo/register.json -o /tmp/r.json --md /tmp/s.md
python3 scripts/report.py /tmp/r.json -o /tmp/report.html --register examples/demo/register.json
python3 scripts/classify.py --text "Устройство кровли из рулонных материалов" --object ordinary
```

Зависимости: Python 3.10+, стандартная библиотека; `jsonschema` — для
полной валидации (без него проверяются только обязательные поля);
`pymupdf` — чтобы рендерить сканы (сам скилл его не импортирует).

## Ограничения, о которых надо сказать пользователю

- Сеть среды закрыта для правовых баз; ключевые нормы ГрК сверены по
  фрагментам текста из поисковой выдачи (поле `verified` в `law.json`),
  Перечень 624 — по консолидированной редакции без сверки; 52 позиции
  со звёздочкой сошлись с разбором заказчика. Флаг `verified_against_official_text`.
- Совокупный размер по ОДО считается только по конкурентным договорам
  (ч. 3 ст. 55.8 и ч. 7 ст. 55.13 в редакции 2026 года сверены по тексту);
  это записано в `scripts/data/policy.json`, вторая выборка — справочно.
- Разделение цены на «СРО/не СРО» — по Перечню 624 как классификатору;
  закон оперирует ценой договора целиком. Оба числа в отчёте рядом.
