# Пакетные файлы Windows обязаны иметь переводы строк CRLF: cmd.exe с
# кириллицей и LF-окончаниями разбирает строки неверно и пытается выполнить
# куски текста как команды.
*.bat text eol=crlf
*.cmd text eol=crlf
*.sh  text eol=lf
