# Зависимости backend'а construction-mcp (см. также pyproject.toml).
mcp>=1.2
jsonschema>=4.18
openpyxl>=3.1
pdfplumber>=0.11
python-docx>=1.1
anyio>=4.0

# Опционально:
# anthropic>=0.40   # реальный мультимодальный анализ кадров (иначе офлайн-заглушка)
# pytest>=8.0       # запуск тестов
